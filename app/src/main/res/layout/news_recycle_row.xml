<?xml version="1.0" encoding="utf-8"?>
<!-- Card layout for a single news article item in the RecyclerView -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_margin="12dp"
    android:elevation="4dp">

    <!-- Container layout with horizontal orientation for image and text -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="4dp"
        android:gravity="center_vertical">

        <!-- News article thumbnail image -->
        <ImageView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            android:id="@+id/article_image_view"/>

        <!-- Vertical layout for title and source -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:orientation="vertical">

            <!-- Title of the news article -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textColor="@color/black"
                android:textStyle="bold"
                android:maxLines="3"
                tools:text="This is the title"
                android:layout_marginBottom="8dp"
                android:id="@+id/article_title"/>
            <!-- Source of the news article -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                tools:text="source"
                android:layout_marginBottom="8dp"
                android:id="@+id/article_source"/>

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>